import { useNavigate } from "react-router-dom";
//import{ HistoryImpl } from "utils";

const NavigateSetter = () => {
    //HistoryImpl.navigate = useNavigate();
    return null;
}

export default NavigateSetter;